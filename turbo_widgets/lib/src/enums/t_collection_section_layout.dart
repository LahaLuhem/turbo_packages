/// Layout options for [TCollectionSection].
enum TCollectionSectionLayout {
  /// Proportional bento layout using TBentoGrid.
  bento,

  /// Vertical list layout with list items.
  list,

  /// Grid layout with configurable dimensions.
  grid,
}
